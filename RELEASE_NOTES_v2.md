# v2 版本发布

## 🎉 新版本特性

### 🚀 主要更新

本次v2版本专注于改进用户交互体验和帮助系统，为用户提供更清晰、准确的命令指导。

### 📋 用户交互命令更新

#### 1. **帮助系统全面改进**
- **全局帮助命令 `/help`**：
  - 重新组织帮助文本结构，提高可读性
  - 明确标注管理员命令和普通用户命令的权限区分
  - 移除误导性的命令别名描述
  
- **详细帮助命令**：
  - 所有命令都支持 `/命令名 help` 格式获取详细说明
  - 增强了每个命令的权限说明和使用示例

#### 2. **核心用户命令**

**🎯 抽人命令 `/pick`**
- 支持多种别名：`/pick`、`/抽人`、`/chouren`
- **功能**：从指定频道随机抽取在线成员
- **参数**：支持频道ID（如 `#1`、`#2`）或留空从所有频道抽取
- **示例**：
  - `/pick` - 从所有频道随机抽取
  - `/pick #1` - 从1号频道随机抽取
  - `/抽人 #2` - 从2号频道随机抽取

**🔮 运势命令 `/fortune`**
- 支持多种别名：`/fortune`、`/运势`、`/今日运势`、`/yunchi`
- **功能**：查看用户的今日运势，包括运势等级、幸运指数和详细说明
- **显示内容**：
  - 运势等级（大吉、吉、凶等）
  - 幸运指数（0-100）
  - 详细运势说明

#### 3. **管理员专用命令**

**🔧 初始化命令 `/init`**
- **支持别名**：`/init`、`/初始化`、`/chushihua`
- **功能**：开发用命令 - 初始化频道映射（仅限管理员）
- **权限**：仅限管理员使用
- **功能说明**：
  - 初始化频道映射，显示所有可用的频道列表
  - 显示频道数量统计和频道映射列表
  - 显示当前在线频道状态

**🔧 刷新管理员命令 `/refadmin`**
- **支持别名**：`/refadmin`、`/刷新管理员`
- **功能**：开发用命令 - 手动刷新管理员列表（仅限管理员）
- **权限**：仅限管理员使用
- **参数**：
  - `/refadmin` - 刷新管理员列表
  - `/refadmin list` - 查看当前管理员名单
- **功能说明**：
  - 从房间成员的频道管理员角色读取管理员名单
  - 刷新后显示当前管理员数量和刷新时间
  - 管理员名单显示格式：`**用户名** (ID: 用户ID)`

#### 4. **管理员特权命令**

**⚡ 强制推送Epic游戏赠送 `/forcepushepic`**
- **支持别名**：`/forcepushepic`、`/forceepic`、`/强制推送epic`
- **功能**：强制推送Epic游戏赠送（仅限管理员）
- **权限**：仅限管理员使用
- **安全机制**：非管理员用户将收到权限提示和用户ID信息

## 🎯 用户体验改进

### 1. **清晰的权限划分**
- **普通用户可用**：`/help`、`/fortune`、`/pick`
- **管理员专用**：`/init`、`/refadmin`、`/forcepushepic`
- **权限提示**：非管理员使用管理员命令时，会收到明确的权限提示

### 2. **多语言支持**
- 所有命令都支持中文和英文别名
- 帮助文本完全中文化，提升用户体验
- 支持多种输入格式，适应不同用户习惯

### 3. **错误处理优化**
- 命令执行错误时的友好提示
- 频道不存在时的明确反馈
- 权限不足时的清晰说明

### 4. **帮助文档完善**
- 每个命令都有详细的使用说明
- 提供实用的使用示例
- 清晰的参数说明和功能描述

## 🔧 技术改进

- **帮助模块化**：新增 `help_methods.py` 模块，统一管理帮助命令
- **命令分发优化**：改进命令识别和分发逻辑
- **权限检查增强**：更严格的权限验证机制
- **多别名支持**：灵活的命令别名识别系统

## 📊 兼容性保证

- **向后兼容**：v1版本的所有功能保持不变
- **接口稳定**：所有API接口保持稳定
- **配置兼容**：现有配置文件完全兼容
- **数据安全**：用户数据和历史记录完全保留

## 🎉 升级建议

1. **新用户**：建议先阅读 `/help` 了解所有可用命令
2. **管理员**：请熟悉新增的管理员命令和权限机制
3. **现有用户**：可继续使用原有命令，新功能将自动生效

## 🚀 后续计划

- 添加更多用户交互功能
- 优化命令响应速度
- 增强错误处理机制
- 扩展管理员权限体系

---

**升级到v2，体验更完善的用户交互系统！** 🎉